%h1
  Videos


.row
  .col-sm-12.table-responsive
    %p.text-muted.float-right Videos are unlocked over time.
    %table.table.table-clickable

      %thead
        %tr
          %th #
          %th Due
          %th Title

      %tbody
        - @videos.each_with_index do |video, index|

          - if index + 1 <= @unlock
            %tr.text-primary{data: {href: "/videos/#{video}"}}
              %td= index + 1
              %td Week #{(index+1) * 2}
              %td #{video.sub(/^\d+\-/,'').titleize}
          - else
            %tr.text-muted
              %td= index + 1
              %td Week #{(index+1) * 2}
              %td #{video.sub(/^\d+\-/,'').titleize}
